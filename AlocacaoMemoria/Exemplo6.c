#include <stdio.h>
#include <stdlib.h>

int main( int argc, const char* argv[] )
{
    int a, b, c;

    printf("Endereco de a: %i \n", &a);
    printf("Endereco de b: %i \n", &b);
    printf("Endereco de c: %i \n", &c);

    return 0;
}
